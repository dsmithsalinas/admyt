# Used only by the local Supabase Edge Function runtime. In production, set this
# with `npx supabase secrets set ANTHROPIC_API_KEY=...`.
ANTHROPIC_API_KEY=
# Rolling 24-hour ceiling for all Anthropic calls made by the chat function.
ANTHROPIC_DAILY_REQUEST_LIMIT=100
